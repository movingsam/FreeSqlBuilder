@using FreeSqlBuilder.Core.Utilities
@model FreeSqlBuilder.TemplateEngine.BuildTask
@{
    Layout = "_Layout.cshtml";
    var project = Model.Project;
    var table = Model.CurrentTable;
}

using System;
using GRES.Framework.Dtos;
namespace @(project.ProjectInfo.ProjectName).@Model.CurrentBuilder.Name
{
    public class @Model.CurrentBuilder.GetName(table.CsName) : PagedDtoBase
    {
    }
}